# Prisma Database Layer

This directory will hold the Jarvis Prisma schema, migrations, and seed scripts.

Database access must stay inside backend services. The gateway must remain stateless and must not import Prisma or any database client.