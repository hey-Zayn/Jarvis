{
  "name": "@jarvis/auth-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.0.0",
    "@grpc/proto-loader": "^0.8.0"
  },
  "devDependencies": { "tsx": "^4.0.0" }
}
